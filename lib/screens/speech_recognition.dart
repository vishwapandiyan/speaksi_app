import 'package:flutter/material.dart';
class SpeechRecognition extends StatelessWidget {
  const SpeechRecognition({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
